name: tk_payment_gateway
description: Tokoin Payment Gateway
version: 0.0.1
homepage: https://www.tokoin.io/

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.15
  flutter_svg: ^0.23.0+1
  uni_links: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:
  assets:
    - assets/